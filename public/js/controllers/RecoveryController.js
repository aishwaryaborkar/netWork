angular.module('RecoveryController', []).controller('RecoveryController', function($scope) {

$scope.handleSubmit = function(){
	alert("Account Credential is send to the registered email.");
}
	

});
